
  {
    "buildCommand": "npm install --legacy-peer-deps && npm run build",
    "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "www" } }
  ]
  }
  