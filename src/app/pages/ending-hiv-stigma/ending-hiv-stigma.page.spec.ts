import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EndingHivStigmaPage } from './ending-hiv-stigma.page';

describe('EndingHivStigmaPage', () => {
  let component: EndingHivStigmaPage;
  let fixture: ComponentFixture<EndingHivStigmaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EndingHivStigmaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
